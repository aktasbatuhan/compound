.env
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
.compound/
artifacts/
*.pyc

*.db
# SQLite write-ahead-log / shared-memory sidecars
*.db-wal
*.db-shm

.claude/worktrees/
.playwright-mcp/

# TypeScript / Bun
node_modules/
dist/
*.tsbuildinfo

# Next.js (dashboard)
.next/

# Local-only design & planning docs — kept on disk, never published.
# Only the top-level README.md is tracked among markdown.
docs/
!site/docs/
# Personal operating notes (GTM/strategy) — never published.
CLAUDE.md
CLAUDE.local.md
SPEC.md
packages/*/README.md

# Local Langfuse experiment harness (real-trace generation) — not published
langfuse-demo/

# MMLU manifest embeds question + answer payload (an eval answer key); keep it
# out of the public repo and regenerate deterministically with
# `python -m compound.bench prepare mmlu` (hash-based partitions are stable).
benchmarks/manifests/mmlu.json

# Ad-hoc local run launchers and previews
_*.sh
glm-charts-preview.png

# Remotion explainer build artifacts
video/node_modules/
video/out/
video/.remotion/

# Local agent tooling and scratch review screenshots — never published.
.agents/
.claude/skills/
skills-lock.json
packages/cli/compound.yaml
*.jpeg
compound-launch-results.png
